/*
    Para compilar manualmente, digite tsc introducao/basico.ts no terminal
    para executar, digite node basico.js

    para executar com o runner do vscode, instale globalmente ts-node => npm i -g ts-node
*/
const str:string = 'Teste TS, oi!';
console.log(str);